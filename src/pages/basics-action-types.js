import React from 'react'
import { Link } from 'gatsby'

import Layout from '../components/layout';
import Sidenav from '../components/sidenav';
import Code from '../components/code';
import BottomNavigation from '../components/bottom-navigation';


const pageTitle = "";

class Page extends React.Component  {

    render(){
        return (
            <Layout breadcrumbs={[{ to: '/', text: 'ooo' }]} pageTitle={"Read Me"}>
                <div className="doc-wrapper">
                    <div className="container">
                        <div id="doc-header" className="doc-header text-center">
                            <h1 className="doc-title"><i className="icon fa fa-paper-plane" /> Action Types</h1>
                            <div className="meta"><i className="far fa-clock" /> Last updated: Oct 30th, 2018</div>
                        </div>{/*//doc-header*/}
                        <div className="doc-body row">
                            <div className="doc-content col-md-9 col-12 order-1">
                                <div className="content-inner">
                                    <section className="doc-section">
                                        <h2 className="section-title">Action Types</h2>
                                        <div className="section-block">
                                            <p>Action types are a native concept in Redux. They are simply strings used to give an action its type.</p>




                                            <Code  code={`
case SOME_ACTION_TYPE:
    return {...}
                                            `} />

                                            <p>There are also used in reducers, and typically in middleware, therefore it is handy to have at your disposal
                                            </p>

                                            <Code  code={`
case SOME_ACTION_TYPE:
return {...}
                                            `} />

                                        </div>
                                    </section>
                                    <section className="doc-section">
                                        <div className="section-block">
                                            <h2>What do they look like here?</h2>
                                            <p>The convention followed by ReduxManagerLib is ModelName/SOME_ACTION, this is why you will need to pass it the name of the model. When working with an action type generated by the library it will look something like this:</p>
                                            <Code  code={`
console.log(BlogPost.actionTypes.CREATE);
// "BlogPost/CREATE"
                                            `} />
                                            <p>Async operations, such as REST calls need to report the end result, this why action types also have stateful version.</p>
                                            <p>The convention used for operation state in action types is: <b>double underscore</b> followed by "SUCCESS" or "FAILURE"</p>

                                            <Code  code={`
"BlogPost/CREATE__SUCCESS"
                                            `} />

                                        </div>
                                    </section>
                                    <section className="doc-section">
                                        <div className="section-block">
                                            <h2>How do I make them?</h2>
                                            <p>ReduxManagerLib exposes factory functions for all basic redux "layers", including action types.</p>
                                            <p>To generate them, simply use the <b>actionTypesFactory</b> function:</p>
                                            <Code code={`
import { actionTypesFactory } from "redux-manager-lib";
let actionTypes = actionTypesFactory("SomeModelName");
                                            `} />

                                            <p>And you are done! </p>
                                            <p><Link to="/api-reference">Check out the API Reference for a more detailed description</Link></p>
                                        </div>
                                    </section>
                                    <div className="col-md-12 col-12">
                                        <BottomNavigation pageTitle={"Action Types"}/>
                                    </div>
                                </div>{/*//content-inner*/}
                            </div>{/*//doc-content*/}
                            <Sidenav/>
                        </div>{/*//doc-body*/}
                    </div>{/*//container*/}
                </div>{/*//doc-wrapper*/}
            </Layout>
        )
    }
}




export default Page
